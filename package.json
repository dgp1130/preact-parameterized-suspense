{
  "name": "preact-ssr",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run -s build && node dist/main.js",
    "build": "tsc"
  },
  "dependencies": {
    "preact": "^10.13.1",
    "preact-render-to-string": "^5.2.6",
    "preact-ssr-prepass": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "typescript": "^5.2.2"
  }
}
